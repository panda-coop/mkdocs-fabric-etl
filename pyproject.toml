[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "mkdocs-fabric-etl"
version = "0.0.0"
description = "MkDocs plugin for Microsoft Fabric ETL documentation"
readme = "README.md"
requires-python = ">=3.9"
authors = [{ name = "Panda Coop" }]
classifiers = [
    "Development Status :: 1 - Planning",
]

[project.urls]
Homepage = "https://github.com/panda-coop/mkdocs-fabric-etl"
